/**
 * @author: Rohit Sethi
 * @description: SQL queries for the UI test
 */

const totalCount = "Total_Count";

// Admin SQL queries
export const dashboardAdmin = {
    totalProjects: `SELECT`,

    assignedProjects: `SELECT`,

    AcknowledgedProjects: `SELECT`,
};
